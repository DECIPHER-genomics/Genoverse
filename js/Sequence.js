CBrowse.Track.Sequence = CBrowse.Track.extend({
  config: {
    labelOverlay   : true,
    separateLabels : false,
    fixedHeight    : false,
    resizable      : false,
    autoHeight     : true,
    spacing        : 0,
    inherit        : [ 'Stranded' ]
  }
});